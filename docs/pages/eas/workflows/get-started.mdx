users (
  id UUID PRIMARY KEY,
  full_name VARCHAR(120),
  email VARCHAR(150) UNIQUE,
  phone VARCHAR(30) UNIQUE,
  password_hash TEXT,

  is_verified BOOLEAN DEFAULT FALSE,
  verification_method VARCHAR(10), -- sms | email

  profile_image TEXT,
  created_at TIMESTAMP,
  updated_at TIMESTAMP
)
